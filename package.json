{
  "name": "nordiqflow",
  "version": "1.1.0",
  "description": "AI-powered career intelligence platform for Sweden",
  "scripts": {
    "dev": "vercel dev",
    "build": "node scripts/prepare-data.js",
    "deploy": "vercel --prod",
    "sync": "node scripts/sync-municipality-data.js",
    "sync:vetlanda": "node scripts/sync-municipality-data.js --municipality 0684",
    "sync:dry": "node scripts/sync-municipality-data.js --dry-run",
    "import": "python scripts/import_jobtech.py"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0"
  },
  "engines": {
    "node": ">=18"
  }
}
